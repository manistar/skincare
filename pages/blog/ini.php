<?php
// $start = 0;
// if(isset($_GET['s'])) {
//   $start = (int)htmlspecialchars($_GET['s']);
// }
// // Ensure $start is non-negative
// $start = max(0, $start);
// $limit = 10; // Number of items per page

// $blog = $d->getall("blog", "status = ? LIMIT $start, $limit", ["1"], fetch: "moredetails");

// $blog = $d->getall("blog", "status = ? && date != ? order by date desc LIMIT $start,  10", ["1", ""], fetch: "moredetails");
?>